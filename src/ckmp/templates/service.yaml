apiVersion: v1
kind: Service
metadata:
  name: {{ include "ckmp.fullname" . }}
  labels:
    {{- include "ckmp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ckmp.selectorLabels" . | nindent 4 }}
