apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ckmp.fullname" . }}-test-connection"
  labels:
    {{- include "ckmp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ckmp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
