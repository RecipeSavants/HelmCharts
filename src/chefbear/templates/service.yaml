apiVersion: v1
kind: Service
metadata:
  name: {{ include "chefbear.fullname" . }}
  labels:
    {{- include "chefbear.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chefbear.selectorLabels" . | nindent 4 }}
