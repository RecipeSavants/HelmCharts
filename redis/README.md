## Redis stack in kubernetes.
Redis bitnami stack

## Installation
Install helm charts:
```
cd redis
helm install stable/redis --values values-production.yaml

```
Redis matser and slave architecture with helm in aks

Its done :)


